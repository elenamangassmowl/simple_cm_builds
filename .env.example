# Path to your CM project directory (absolute path, no trailing slash)
PROJECT_DIR=<PROJECT_DIR>

# Default build environment (dev, prod, test, etc.)
ENV=<ENV>

# Default application version (used for output artifacts)
VERSION=<VERSION>